PORT=
DATABASE_URL="mongodb://root:prisma@localhost:27017/sesami-app?authSource=admin&directConnection=true"
SESAMI_CLIENT_ID=
SESAMI_CLIENT_SECRET=
APP_DOMAIN=

# Sesami proxy auth: env (read/write API keys) | offline (per-shop OAuth token from DB)
SESAMI_AUTH_MODE=env

# Sesami Admin API auth (temporary; will move later)
read_SESAMI_API_KEY=
read_SESAMI_CLIENT_ID=
sesami_ADMIN_SHOP_ID=

write_SESAMI_API_KEY=
write_SESAMI_CLIENT_ID=

